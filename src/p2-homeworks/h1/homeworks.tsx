import React from 'react'

let homeworks = (a: number) => {
    return (
        <div style={{color: 'rgb(235, 178, 34)'}} >
            {`homeworks${a}` }
        </div>

    )
} 

export default homeworks

